When running PHP for this project, use the Docker Compose web service, e.g. `docker compose exec web php /var/www/html/path/to/script.php`, because host PHP may be unavailable or mismatched.
Use `docker compose` rather than the legacy `docker-compose` command in new instructions and commands.
